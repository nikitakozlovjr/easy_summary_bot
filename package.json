{
  "name": "easy_summary_bot",
  "version": "1.0.0",
  "description": "A Telegram bot, which receives a link to a site, returns a pdf file, which is an annotation of the site in an easy-to-read and save format.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
